[project]
name = "kugelblitz-eval"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "langfuse>=3.0",
    "openai>=1.0",
    "pandas>=2.0",
    "pyarrow>=15.0",
    "pyyaml>=6.0",
    "rich>=13.0",
    "pydantic>=2.0",
]

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]
